#ifndef HEADER_H
#define HEADER_H

#include <iostream>
#include <string>
#include <fstream>
#include <QVector>
#include <QMap>

using std::string;
using std::cout;
using std::endl;

namespace std {
    bool is_digit(const string&);
    double to_double(const string&);
}

using ll = long long;
using ull = unsigned long long;
#endif // HEADER_H
